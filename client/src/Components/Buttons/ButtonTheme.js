import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#e53935",
      contrastText: "#fff",
    },
    secondary: {
      main: "#1e88e5",
      contrastText: "#fff",
    },
    neutral: {
      main: "#fdd835",
      contrastText: "#000",
    },
  },
});
